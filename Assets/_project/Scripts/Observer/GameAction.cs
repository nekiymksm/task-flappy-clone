namespace _project.Scripts.Observer
{
    public enum GameAction
    {
        ButtonPressed,
        CharacterJump,
        CharacterCollide,
        GameStart,
        GameRestart,
        GameEnd,
        TakePoint,
        DisableSounds,
        EnableSounds,
        DisableMusic,
        EnableMusic
    }
}